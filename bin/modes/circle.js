"use strict";var colors=require("../colors");var pos=require("cli-position");module.exports = {name:"circle",run:function run(){var angle=0;var colorIndex=0;setInterval(function(){if(angle > 2 * Math.PI){angle = 0;}var centerX=Math.round(pos.columns() / 2);var centerY=Math.round(pos.rows() / 2);var radius=centerX < centerY?centerX:centerY;pos.center();var xOffset=Math.round(Math.sin(angle) * (radius * 2));var yOffset=Math.round(Math.cos(angle) * radius);pos.moveLeft(xOffset).moveUp(yOffset);process.stdout.write(colors[colorIndex % colors.length] + "m ");angle += 0.04;colorIndex++;},3);}};