"use strict";var pos=require("cli-position");var colors=require("../colors");var scale=require("linear-scale");module.exports = {name:"sine",run:function run(){pos.center();var xOffset=0;setInterval(function(){var columns=pos.columns();var yTop=pos.rows() / 3;var yCenter=pos.rows() / 2;var xScale=scale([0,columns],[0,4 * Math.PI]);console.log('`\x1b[m\x1b[2J');for(var x=0;x < columns;x++) {var y=Math.round(Math.sin(xScale(x + xOffset)) * yTop + yCenter);pos.moveTo(x,y);process.stdout.write(colors[x % colors.length] + " ");}xOffset++;},20);}};